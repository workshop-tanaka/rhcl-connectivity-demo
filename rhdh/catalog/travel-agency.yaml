# ===========================================================================
# Catalogo RHDH da demo RHCL — travel-agency.
#
# Modela a demo em duas Systems porque as policies do Kuadrant tem escopos
# diferentes e isso importa na hora de explicar:
#   - rhcl-ingress   : Gateway + policies com targetRef no Gateway (valem para
#                      TODA rota anexada -- deny-all, rate limit baixo, DNS, TLS)
#   - travel-agency  : app + policies com targetRef na HTTPRoute (valem so para
#                      esta API -- API key e rate limit por usuario)
#
# ${DEMO_API_HOST} e ${DEMO_ECHO_HOST} sao substituidos pelo setup-catalog.sh
# com os hostnames reais lidos das HTTPRoutes do cluster.
# ===========================================================================
# ---------------------------------------------------------------------------
# O usuario 'guest'.
#
# O provider guest emite a identidade user:development/guest -- namespace
# 'development', que vem de auth.environment, e NAO 'default'. Sem esta
# entidade o portal funciona (dangerouslyAllowSignInWithoutUserInCatalog
# permite entrar), mas todo consumidor da identity API recebe 404:
#
#   GET /api/catalog/entities/by-name/user/development/guest -> 404
#
# E o que se suspeita bloquear o fluxo de pedido de API key do plugin Kuadrant,
# que emite chave por usuario. Criar a entidade e mais barato que ligar RBAC.
#
# Ao trocar guest por um IdP real, esta entidade sai junto com o provider.
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: guest
  namespace: development
  description: Identidade emitida pelo provider guest. Lab apenas.
spec:
  profile:
    displayName: Guest
    # O e-mail NAO e decorativo: o plugin Kuadrant desabilita o botao
    # 'Request API Access' sem ele e mostra "Email address is required" ao lado.
    # A chave emitida fica associada a este endereco.
    email: guest@travel-agency.demo
  memberOf:
    - group:default/platform-team
---
# ---------------------------------------------------------------------------
# AS PERSONAS DO ATO 6 — as mesmas que existem como usuario no GitLab.
#
# Elas existem por causa do resolver de sign-in: 'usernameMatchingUserEntityName'
# casa o username do GitLab com User:default/<nome>. Sem a entidade, o login
# acontece e a sessao morre logo depois em "user not found" -- falha que parece
# de credencial e e de catalogo.
#
# NAMESPACE default de proposito (o 'guest' acima esta em 'development'): o
# resolver procura em default, e entidade no namespace errado e invisivel para
# ele.
#
# O e-mail difere do que esta no GitLab (@example.invalid, que nunca recebe
# correio). Nao e inconsistencia: o resolver casa por USERNAME, e os dois
# sistemas nao precisam concordar no endereco. Aqui vale a convencao do repo,
# que e o que aparece na tela.
#
# E o e-mail nao e decorativo, pelo mesmo motivo do 'guest' acima: sem ele o
# plugin Kuadrant desabilita 'Request API Access'.
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: globex-travel
  description: Parceiro tier gold. Abre merge request de assinatura no GitLab.
spec:
  profile:
    displayName: Globex Travel
    email: globex@travel-agency.demo
  memberOf:
    - group:default/parceiros
---
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: initech-voyages
  description: Parceiro tier silver. Abre merge request de assinatura no GitLab.
spec:
  profile:
    displayName: Initech Voyages
    email: initech@travel-agency.demo
  memberOf:
    - group:default/parceiros
---
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: acme-trips
  description: Parceiro tier free. Abre merge request de assinatura no GitLab.
spec:
  profile:
    displayName: ACME Trips
    email: acme@travel-agency.demo
  memberOf:
    - group:default/parceiros
---
# Quem APROVA. Maintainer em rhcl/apis no GitLab, e o unico que faz merge --
# quem pede nao e quem aprova.
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: plat-eng
  description: Engenharia de plataforma. Aprova e faz merge das assinaturas.
spec:
  profile:
    displayName: Plataforma
    email: plataforma@travel-agency.demo
  memberOf:
    - group:default/platform-team
---
apiVersion: backstage.io/v1alpha1
kind: Group
metadata:
  name: parceiros
  description: Consumidores da API. Abrem assinatura por merge request; nao aprovam.
spec:
  type: team
  children: []
---
apiVersion: backstage.io/v1alpha1
kind: Group
metadata:
  name: platform-team
  description: Time responsavel pela plataforma e pelas policies de conectividade.
spec:
  type: team
  children: []
---
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: travel
  description: Dominio de negocio da demo de conectividade.
spec:
  owner: group:default/platform-team
---
# ---------------------------------------------------------------------------
# System: borda (RHCL / Kuadrant)
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: rhcl-ingress
  description: >-
    Borda gerenciada pelo Red Hat Connectivity Link: o Gateway compartilhado e
    as policies de escopo de gateway.
spec:
  owner: group:default/platform-team
  domain: travel
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: prod-web
  title: Gateway prod-web
  description: Gateway compartilhado (gatewayClass istio), listener HTTPS em *.travels.
  links:
    - url: https://${DEMO_API_HOST}
      title: API travel-agency
      icon: web
    - url: https://${CONSOLE_HOST}/k8s/ns/ingress-gateway/gateway.networking.k8s.io~v1~Gateway/prod-web
      title: Abrir no console
      icon: web
spec:
  type: gateway
  owner: group:default/platform-team
  system: rhcl-ingress
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: prod-web-deny-all
  title: AuthPolicy — deny all na borda
  description: >-
    AuthPolicy no Gateway. Nega tudo por padrao -- uma rota so passa a responder
    depois que alguem anexa uma AuthPolicy propria a ela. E o default seguro da
    borda, nao uma policy da aplicacao.
  links:
    - url: https://${CONSOLE_HOST}/k8s/ns/ingress-gateway/kuadrant.io~v1~AuthPolicy/prod-web-deny-all
      title: Abrir no console
      icon: web
spec:
  type: kuadrant-authpolicy
  owner: group:default/platform-team
  system: rhcl-ingress
  dependencyOf:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: ingress-gateway-rlp-lowlimits
  title: RateLimitPolicy — teto do gateway
  description: >-
    RateLimitPolicy no Gateway: 50 req / 10s. Teto de protecao da borda; a
    policy da rota (2000 / 10s por usuario) e mais permissiva, entao na pratica
    quem corta primeiro e este limite do gateway.
  links:
    - url: https://${CONSOLE_HOST}/k8s/ns/ingress-gateway/kuadrant.io~v1~RateLimitPolicy/ingress-gateway-rlp-lowlimits
      title: Abrir no console
      icon: web
spec:
  type: kuadrant-ratelimitpolicy
  owner: group:default/platform-team
  system: rhcl-ingress
  dependencyOf:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: prod-web-dnspolicy
  title: DNSPolicy — registros do gateway
  description: >-
    DNSPolicy: publica os registros do Gateway no provedor DNS (Route 53) a
    partir das credenciais em prod-web-aws-credentials. NAO ESTA APLICADA neste
    cluster -- aqui o Gateway usa o wildcard que o cluster ja tem. Emitir por
    DNS01 para o hostname do proprio Gateway derruba a resolucao dele, e o
    certificado ainda assim sai Ready=True, o que faz o diagnostico apontar
    para o lugar errado.
  links:
    - url: ${DEMO_REPO_URL_BLOB}/platform-reference/policies-connectivity/prod-web-dnspolicy.yaml
      title: Manifesto (nao aplicado neste cluster)
      icon: web
spec:
  type: kuadrant-dnspolicy
  owner: group:default/platform-team
  system: rhcl-ingress
  dependencyOf:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: prod-web-tls-policy
  title: TLSPolicy — certificado do listener
  description: >-
    TLSPolicy: emite o certificado do listener via ClusterIssuer do cert-manager.
    Vive em platform-reference/ -- e da plataforma, nao da demo. NAO ESTA
    APLICADA neste cluster, pelo mesmo motivo da DNSPolicy: o caminho DNS01
    quebra o proprio host do Gateway.
  links:
    - url: ${DEMO_REPO_URL_BLOB}/platform-reference/policies-connectivity/prod-web-tls-policy.yaml
      title: Manifesto (nao aplicado neste cluster)
      icon: web
spec:
  type: kuadrant-tlspolicy
  owner: group:default/platform-team
  system: rhcl-ingress
  dependencyOf:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: prod-web-telemetry
  title: TelemetryPolicy — rotulos de plano e parceiro
  description: >-
    TelemetryPolicy no Gateway: rotula as metricas com o plano (auth.kuadrant.plan)
    e o parceiro (auth.demo.partner). O segundo so existe porque a AuthPolicy
    exporta a identidade como dynamic metadata -- no contexto CEL do WasmPlugin
    nao ha 'auth.identity', entao sem essa exportacao o label sai vazio.
  links:
    - url: https://${CONSOLE_HOST}/k8s/ns/ingress-gateway/extensions.kuadrant.io~v1alpha1~TelemetryPolicy/prod-web-telemetry
      title: Abrir no console
      icon: web
spec:
  type: kuadrant-telemetrypolicy
  owner: group:default/platform-team
  system: rhcl-ingress
  dependsOn:
    - resource:default/travel-agency-authpolicy
  dependencyOf:
    - resource:default/prod-web
---
# ---------------------------------------------------------------------------
# System: aplicacao
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: travel-agency
  description: Aplicacao de exemplo exposta pelo Gateway prod-web.
  annotations:
    # O que enche a secao Docs do portal. Sem esta anotacao o TechDocs esta
    # ligado e nao tem o que renderizar -- a secao aparece vazia.
    # O mkdocs.yml na raiz do repo define o nav; o RHDH clona e gera no pod.
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
  # Nao ha plugin de Grafana instalavel no RHDH 1.10 -- nem no ghcr (so builds
  # de PR) nem no npm (o pacote nao traz build dinamico). O link direto ao
  # dashboard resolve o mesmo problema numa demo: sair do portal para o numero.
  links:
    - url: https://${GRAFANA_HOST}/d/rhcl-planos
      title: Planos comerciais (Grafana)
      icon: dashboard
spec:
  owner: group:default/platform-team
  domain: travel
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: travel-agency-authpolicy
  title: AuthPolicy — API key na rota
  description: >-
    AuthPolicy na HTTPRoute travel-agency. Aceita API key da query string
    (?APIKEY=) a partir de Secrets com label app=partner em kuadrant-system, e
    sobrepoe o deny-all do Gateway para esta rota. Tambem exporta a identidade
    como dynamic metadata (namespace 'demo'), que e o que permite a
    TelemetryPolicy rotular metrica por parceiro.
  links:
    - url: https://${CONSOLE_HOST}/k8s/ns/travel-agency/kuadrant.io~v1~AuthPolicy/travel-agency-authpolicy
      title: Abrir no console
      icon: web
spec:
  type: kuadrant-authpolicy
  owner: group:default/platform-team
  system: travel-agency
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: travels-plans
  title: PlanPolicy — os tres tiers
  description: >-
    PlanPolicy na HTTPRoute travel-agency: classifica cada API key em um tier
    pelo label kuadrant.io/plan-id e aplica limites por tier --
    gold 30/10s (5000/dia), silver 10/10s (500/dia), free 3/10s (50/dia) e um
    catch-all 'unclassified' de 1/60s. No 1.4 ele NAO convive com a RateLimitPolicy
    plana: seria sobreposto por ela, e por isso o overlay a remove.
  links:
    - url: https://${CONSOLE_HOST}/k8s/ns/travel-agency/extensions.kuadrant.io~v1alpha1~PlanPolicy/travels-plans
      title: Abrir no console
      icon: web
spec:
  type: kuadrant-planpolicy
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - resource:default/travel-agency-authpolicy
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: ratelimit-policy-travels
  title: RateLimitPolicy plana — removida no 1.4
  description: >-
    RateLimitPolicy plana (2000 req / 10s por auth.identity.userid). NAO ESTA
    APLICADA neste cluster: o overlay env/rhcl-1.4_ocp-4.21 a remove com
    $patch: delete. A base/ fica intacta, porque continua correta para o 1.2.

    O motivo e uma inversao de precedencia entre releases. No 1.2.1 o PlanPolicy
    sobrepunha esta policy, e era esse status que provava a hierarquia. No 1.4.2
    inverteu: com ela presente, a RLP gerada pelo PlanPolicy fica Enforced=False
    "overridden by travel-agency/ratelimit-policy-travels", o PlanPolicy fica
    Accepted=False, e os tres tiers param de existir -- com tudo respondendo 200,
    sem sintoma nenhum no caminho de dados.

    A cena de precedencia passou a ser contada com ingress-gateway-rlp-lowlimits,
    que mira o Gateway e cede para as policies de rota: hierarquia
    gateway-vs-rota, que continua valendo no 1.4.
  links:
    - url: ${DEMO_REPO_URL_BLOB}/base/policies-traffic/ratelimit-policy-travels.yaml
      title: Manifesto (nao aplicado neste cluster)
      icon: web
spec:
  type: kuadrant-ratelimitpolicy
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - resource:default/travel-agency-authpolicy
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: travels
  description: Frontend da agencia; agrega os demais servicos. Unico exposto pela HTTPRoute.
  annotations:
    backstage.io/kubernetes-label-selector: app=travels
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    # A aba 'Imagem' do plugin Quay so aparece com esta anotacao. Ela
    # aponta para um repositorio PUBLICO de verdade: os servicos da demo
    # rodam as imagens do proprio Kiali, entao a aba mostra tags e data
    # de push reais, e nao um exemplo vazio.
    quay.io/repository-slug: kiali/demo_travels_travels
    # ATIVA. O @backstage-community/plugin-jaeger nao esta no registry de
    # overlays da Red Hat, entao passou pelo 'yarn export-dynamic' e vive no
    # plugin-registry interno -- ver o bloco WITH_JAEGER do setup-plugins.sh.
    #
    # Fica pronta desde ja porque o valor NAO e adivinhavel: e o nome que o
    # Tempo conhece, no formato <servico>.<namespace>, e foi lido da API dele
    # em 2026-08-27 (/api/traces/v1/dev/api/services). Errar aqui produz uma
    # aba que abre vazia, que no palco parece 'nao ha trafego'.
    #
    # A versao que casa e a 0.15.0, cujo workspace mira Backstage 1.49.2 -- a
    # linha do 1.49.4 que o RHDH 1.10.3 embute. A 0.9.0 parece casar melhor
    # pelo range que declara no npm, e nao casa: o range publicado engana, e
    # quem decide e o backstage.json do WORKSPACE.
    jaegertracing.io/service: travels.travel-agency
    # 168h (sete dias), e nao o default de 1h. Num cluster sem trafego recente a
    # janela de 1h deixa a aba em 'No records to display' -- que na frente de um
    # cliente le-se como 'a instrumentacao nao funciona', e nao como 'ninguem
    # chamou a API hoje'.
    #
    # EM HORAS, obrigatoriamente. O plugin valida com /^(\d+)([mh])$/ e nao
    # conhece 'd': '7d' derruba a aba inteira com "Invalid time format", e o
    # erro estoura ANTES de qualquer requisicao -- entao nada do Tempo, do
    # proxy ou do token aparece no diagnostico. Custou uma investigacao em
    # 2026-08-27.
    #
    # Sete dias e generoso de proposito: a retencao real do Tempo aqui nao esta
    # declarada, e pedir alem do que existe nao custa nada -- o backend devolve
    # o que tem. Cravar um numero 'realista' seria codificar um palpite.
    #
    # A janela larga e a rede de seguranca; o trafego ao vivo do roteiro e o que
    # mostra o dado se mexendo. As duas juntas: nunca vazia, e viva quando
    # importa.
    jaegertracing.io/lookback: 168h
    # Consulta de UMA palavra, de proposito: o plugin trata selector de uma
    # palavra como busca por TAG e qualquer outra coisa como busca por titulo.
    # Por isso os tres dashboards autorais levam "tags": ["rhcl"] -- assim um
    # dashboard novo aparece nos componentes so por ganhar a tag, sem mexer
    # aqui. Os de persona vem do upstream do Kuadrant e ficam de fora: tem job
    # anti-drift no CI, e etiquetar quebraria a comparacao.
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — travels.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  # A entidade travels-api e ingerida pelo plugin Kuadrant a partir do
  # APIProduct do developer portal -- nao esta neste arquivo. O provider nao le
  # 'system' de anotacao nenhuma, entao a unica forma de tirar aquela entidade
  # do isolamento no grafo e declarar a relacao DAQUI: providesApis gera o
  # apiProvidedBy do outro lado, e a API passa a ser alcancavel pelo System.
  providesApis:
    - travels-api
  dependsOn:
    - component:default/flights
    - component:default/hotels
    - component:default/cars
    - component:default/insurances
    - resource:default/travel-agency-authpolicy
    - resource:default/ratelimit-policy-travels
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: flights
  description: Servico de voos.
  annotations:
    backstage.io/kubernetes-label-selector: app=flights
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    quay.io/repository-slug: kiali/demo_travels_flights
    jaegertracing.io/service: flights.travel-agency
    jaegertracing.io/lookback: 168h
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — flights.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - component:default/discounts
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: hotels
  description: Servico de hoteis.
  annotations:
    backstage.io/kubernetes-label-selector: app=hotels
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    quay.io/repository-slug: kiali/demo_travels_hotels
    jaegertracing.io/service: hotels.travel-agency
    jaegertracing.io/lookback: 168h
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — hotels.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - component:default/discounts
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cars
  description: Servico de locacao de veiculos.
  annotations:
    backstage.io/kubernetes-label-selector: app=cars
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    quay.io/repository-slug: kiali/demo_travels_cars
    jaegertracing.io/service: cars.travel-agency
    jaegertracing.io/lookback: 168h
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — cars.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - component:default/discounts
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: insurances
  description: Servico de seguros.
  annotations:
    backstage.io/kubernetes-label-selector: app=insurances
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    quay.io/repository-slug: kiali/demo_travels_insurances
    jaegertracing.io/service: insurances.travel-agency
    jaegertracing.io/lookback: 168h
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — insurances.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  dependsOn:
    - component:default/discounts
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: discounts
  description: >-
    Servico de descontos, com duas versoes em paralelo (v1 e v2) atras do mesmo
    Service -- e o alvo natural para demonstrar roteamento por peso na HTTPRoute.
  annotations:
    backstage.io/kubernetes-label-selector: app=discounts
    backstage.io/kubernetes-namespace: travel-agency
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: travel-agency
    quay.io/repository-slug: kiali/demo_travels_discounts
    jaegertracing.io/service: discounts.travel-agency
    jaegertracing.io/lookback: 168h
    grafana/dashboard-selector: rhcl
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — discounts.travel-agency
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: echo-api
  description: >-
    Servico de eco em namespace separado, anexado ao mesmo Gateway. Serve para
    mostrar o deny-all da borda: sem AuthPolicy propria, esta rota nao responde.
  links:
    # Dev Spaces: abre o repositorio DESTA demo num IDE em container, com um
    # clique a partir do componente. Nao confundir com o decorator "edit code"
    # da aba Topology: o destino e o mesmo repositorio, mas o lapis leva ao
    # GitHub, nunca ao IDE -- o fetch do plugin Kubernetes e restrito ao
    # kubernetes-namespace da entidade e o CheCluster vive em
    # openshift-devspaces. Medido; ver platform-reference/devspaces/README.md.
    # Este link e o caminho que esta ligado, e nao depende do Topology carregar.
    # Se o Dev Spaces nao estiver instalado, o setup-catalog.sh apaga estas
    # linhas em vez de publicar um link morto.
    - url: https://${DEVSPACES_HOST}/#${DEMO_REPO_URL}
      title: Abrir no Dev Spaces
      icon: code
    - url: https://${CONSOLE_HOST}/observe/traces
      title: Traces — echo-api.echo-api
      icon: dashboard
    - url: https://${DEMO_ECHO_HOST}
      title: Endpoint
      icon: web
  annotations:
    backstage.io/kubernetes-label-selector: app.kubernetes.io/name=echo-api
    backstage.io/kubernetes-namespace: echo-api
    # O Kiali usa a anotacao DELE, nao a do Kubernetes.
    kiali.io/namespace: echo-api
    # View Source e View TechDocs no card About: sem estas duas anotacoes os
    # botoes aparecem desabilitados. source-location termina em / porque
    # aponta para um diretorio, nao para um arquivo.
    backstage.io/source-location: url:${DEMO_REPO_URL}/
    backstage.io/techdocs-ref: url:${DEMO_REPO_URL}
    # Volta agora que o repo tem CI: o workflow valida os manifests DESTE
    # componente, entao a aba Actions passa a ter conteudo proprio.
    # A aba Kiali usa o mesmo namespace; o provider e o nome do
    # bloco kiali.providers no app-config.
    kiali.io/provider: default
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-team
  system: rhcl-ingress
---
# ---------------------------------------------------------------------------
# Consumidores da API.
#
# Cada um corresponde a uma API key em base/identity/apikeys.yaml. O tier nao
# vem da chave em si: vem do label kuadrant.io/plan-id nela, que o PlanPolicy
# le no predicate. Uma chave 'app: partner' SEM esse label cai no plano
# catch-all -- e a falha e silenciosa.
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: globex-travel
  title: Globex Travel
  description: Parceiro tier gold — 30 req/10s, 5000/dia (apikey-gold-globex).
  # Os parceiros nao tem servico nem trace proprio -- sao consumidores. O que
  # existe deles e consumo, e e exatamente o que o dashboard rhcl-parceiros
  # mede. A tag 'rhcl' traz os tres dashboards autorais para a pagina de cada
  # um; o de consumo e o que faz sentido aqui.
  annotations:
    grafana/dashboard-selector: rhcl
spec:
  type: api-consumer
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  consumesApis:
    - travels-api
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: initech-voyages
  title: Initech Voyages
  description: Parceiro tier silver — 10 req/10s, 500/dia (apikey-silver-initech).
  # Os parceiros nao tem servico nem trace proprio -- sao consumidores. O que
  # existe deles e consumo, e e exatamente o que o dashboard rhcl-parceiros
  # mede. A tag 'rhcl' traz os tres dashboards autorais para a pagina de cada
  # um; o de consumo e o que faz sentido aqui.
  annotations:
    grafana/dashboard-selector: rhcl
spec:
  type: api-consumer
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  consumesApis:
    - travels-api
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: acme-trips
  title: ACME Trips
  description: Parceiro tier free (avaliacao) — 3 req/10s, 50/dia (apikey-free-acme).
  # Os parceiros nao tem servico nem trace proprio -- sao consumidores. O que
  # existe deles e consumo, e e exatamente o que o dashboard rhcl-parceiros
  # mede. A tag 'rhcl' traz os tres dashboards autorais para a pagina de cada
  # um; o de consumo e o que faz sentido aqui.
  annotations:
    grafana/dashboard-selector: rhcl
spec:
  type: api-consumer
  lifecycle: production
  owner: group:default/platform-team
  system: travel-agency
  consumesApis:
    - travels-api
---
# =============================================================================
# A PLATAFORMA COMO ENTIDADE
#
# Ate 2026-08-27 o catalogo conhecia as policies e ignorava tudo o que as
# executa. O efeito no palco era um catalogo que descrevia regras sem mostrar
# onde elas rodam -- e a pergunta "onde isso e aplicado?" so tinha resposta no
# console.
#
# Cada Resource abaixo corresponde a um objeto REAL deste cluster, declarado na
# anotacao rhcl.demo/cluster-object no formato <recurso>/<namespace>/<nome>. O
# setup-catalog.sh confere essa anotacao antes de publicar e descarta o que nao
# existir -- mesma disciplina que ja valia para as policies, agora generica.
# =============================================================================
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: plataforma
  title: Plataforma
  description: >-
    O que executa as policies e observa o trafego: plano de controle e de dados
    do Connectivity Link, Service Mesh, observabilidade e as ferramentas de
    entrega. Nao e a demo -- e o que a sustenta.
spec:
  owner: group:default/platform-team
  domain: travel
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: kuadrant-control-plane
  title: Kuadrant — plano de controle
  description: >-
    O CR que liga o Connectivity Link. E ele que reconcilia toda policy deste
    catalogo: sem ele Accepted nunca vira True, e as regras ficam declaradas sem
    efeito nenhum no trafego.
  annotations:
    rhcl.demo/cluster-object: kuadrant/kuadrant-system/kuadrant
    backstage.io/kubernetes-namespace: kuadrant-system
spec:
  type: control-plane
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: authorino
  title: Authorino — onde a autenticacao roda
  description: >-
    Servico externo de autorizacao que o Gateway consulta a cada requisicao. As
    AuthPolicy nao validam nada por si: elas configuram este processo. Quando ele cai,
    a API responde 500 no lugar de 401 -- e a policy nao tem culpa.
  annotations:
    rhcl.demo/cluster-object: authorino/kuadrant-system/authorino
    backstage.io/kubernetes-namespace: kuadrant-system
spec:
  type: auth-service
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: limitador
  title: Limitador — onde o limite e contado
  description: >-
    Contador de requisicoes que o Gateway consulta. As RateLimitPolicy e o PlanPolicy
    descrevem os limites; quem os conta e este servico. E o unico ponto onde 'tier
    gold' vira um numero.
  annotations:
    rhcl.demo/cluster-object: limitador/kuadrant-system/limitador
    backstage.io/kubernetes-namespace: kuadrant-system
spec:
  type: rate-limit-service
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: istio-control-plane
  title: Service Mesh — plano de controle
  description: >-
    OpenShift Service Mesh 3. Fornece a gatewayClass que o Gateway prod-web usa e
    injeta o sidecar nativo dos servicos -- um initContainer com restartPolicy:
    Always, e nao um container comum.
  annotations:
    rhcl.demo/cluster-object: istio/default/default
    backstage.io/kubernetes-namespace: default
spec:
  type: service-mesh
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: tempo
  title: Tempo — armazenamento de traces
  description: >-
    Guarda os traces que a aba Traces le. Serve por tenant: o caminho da API termina
    em /api/traces/v1/dev, e o tenant faz parte da URL, nao um cabecalho.
  annotations:
    rhcl.demo/cluster-object: tempomonolithic/tracing-system/tempo
    backstage.io/kubernetes-namespace: tracing-system
spec:
  type: tracing-backend
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: otel-collector
  title: OpenTelemetry Collector
  description: >-
    Recebe os spans do Gateway e os encaminha ao Tempo. E o elo que a TelemetryPolicy
    alimenta -- sem ele a policy fica correta e a aba, vazia.
  annotations:
    rhcl.demo/cluster-object: opentelemetrycollector/tracing-system/otel
    backstage.io/kubernetes-namespace: tracing-system
spec:
  type: otel-collector
  owner: group:default/platform-team
  system: plataforma
  dependsOn:
    - resource:default/tempo
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: kiali
  title: Kiali — console do Service Mesh
  description: >-
    Topologia e trafego da malha. Le as metricas do Thanos. A estrategia de
    autenticacao dele e uma escolha com consequencia: 'openshift' quebra a aba do
    plugin no portal, que so implementa serviceAccountToken.
  annotations:
    rhcl.demo/cluster-object: kiali/istio-system/kiali
    backstage.io/kubernetes-namespace: istio-system
spec:
  type: observability-console
  owner: group:default/platform-team
  system: plataforma
  dependsOn:
    - resource:default/istio-control-plane
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: grafana
  title: Grafana — dashboards
  description: >-
    Serve os dashboards que aparecem nos componentes pelo plugin. Aqui roda com acesso
    anonimo como Admin, o que e o oposto do Kiali e do Tempo -- e por isso o proxy do
    portal fala com ele sem token.
  annotations:
    rhcl.demo/cluster-object: grafana/monitoring/grafana
    backstage.io/kubernetes-namespace: monitoring
spec:
  type: dashboards
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: openshift-gitops
  title: OpenShift GitOps (Argo CD)
  description: >-
    Reconcilia o que esta em git com o que esta no cluster. E o mecanismo que torna
    literal a frase do roteiro sobre a mudanca ficar 'em git, com autor e data' -- o
    merge da MR e o que dispara a aplicacao.
  annotations:
    rhcl.demo/cluster-object: argocd/openshift-gitops/openshift-gitops
    backstage.io/kubernetes-namespace: openshift-gitops
spec:
  type: gitops
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: gitlab
  title: GitLab — SCM e IdP
  description: >-
    Guarda o codigo E autentica o portal. Um mecanismo em vez de dois: o mesmo login
    que diz quem a pessoa e tambem entrega o token com que o scaffolder age em nome
    dela.
  annotations:
    rhcl.demo/cluster-object: gitlab/gitlab-system/gitlab
    backstage.io/kubernetes-namespace: gitlab-system
spec:
  type: scm
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: keycloak
  title: Red Hat build of Keycloak
  description: >-
    IdP do developer portal do Connectivity Link. Chega junto com o RHCL 1.4.2 e e o
    que sustenta o autoatendimento de chaves do Ato 6.
  annotations:
    rhcl.demo/cluster-object: keycloak/keycloak/keycloak
    backstage.io/kubernetes-namespace: keycloak
spec:
  type: idp
  owner: group:default/platform-team
  system: plataforma
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: travel-agency-route
  title: HTTPRoute — travel-agency
  description: >-
    A rota que expoe a API pelo Gateway. E o alvo de quase toda policy da demo:
    AuthPolicy, PlanPolicy e RateLimitPolicy miram a ROTA, e por isso vencem as
    policies de Gateway -- hierarquia rota-vence-gateway, que vale no 1.4.
  annotations:
    rhcl.demo/cluster-object: httproute/travel-agency/travel-agency
    backstage.io/kubernetes-namespace: travel-agency
spec:
  type: httproute
  owner: group:default/platform-team
  system: rhcl-ingress
  dependsOn:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: echo-api-route
  title: HTTPRoute — echo-api
  description: >-
    Segunda rota no mesmo Gateway. Existe para provar que a borda e compartilhada e
    que cada rota governa a si mesma, sem uma interferir na outra.
  annotations:
    rhcl.demo/cluster-object: httproute/echo-api/echo-api
    backstage.io/kubernetes-namespace: echo-api
spec:
  type: httproute
  owner: group:default/platform-team
  system: rhcl-ingress
  dependsOn:
    - resource:default/prod-web
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: api-tls
  title: Certificado do listener
  description: >-
    O certificado do Gateway. E copia do wildcard que o cluster ja tem, e NAO vem de
    TLSPolicy com DNS01 -- aquele caminho emite um certificado Ready=True e derruba a
    resolucao do proprio host.
  annotations:
    rhcl.demo/cluster-object: secret/ingress-gateway/api-tls
    backstage.io/kubernetes-namespace: ingress-gateway
spec:
  type: certificate
  owner: group:default/platform-team
  system: rhcl-ingress
