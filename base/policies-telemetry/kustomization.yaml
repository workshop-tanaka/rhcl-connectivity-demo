apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# TelemetryPolicy: dimensoes de negocio nas metricas (extensions.kuadrant.io).
# Telemetry (Istio): a dimensao que o Kuadrant nao consegue dar -- por parceiro.
resources:
  - prod-web-telemetry.yaml
  - istio-partner-dimension.yaml
  - istio-tracing-travel-agency.yaml
