apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# AuthPolicies. NOTA: prod-web-deny-all tem rego "allow = true" (bug herdado:
# o nome diz deny mas libera). Corrigido na Fase A via patch em overlay,
# preservando o original aqui para o "antes/depois" do roteiro.
resources:
  - prod-web-deny-all.yaml
  - travel-agency-authpolicy.yaml
