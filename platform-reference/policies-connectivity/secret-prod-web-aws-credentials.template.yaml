apiVersion: v1
data:
  AWS_ACCESS_KEY_ID: REDACTED_BASE64_SET_AT_DEPLOY
  AWS_SECRET_ACCESS_KEY: REDACTED_BASE64_SET_AT_DEPLOY
kind: Secret
metadata:
  name: prod-web-aws-credentials
  namespace: ingress-gateway
  annotations:
    rhcl-demo/note: Valores redigidos. Preencha no deploy via oc create secret, Sealed Secrets ou ExternalSecrets.
type: kuadrant.io/aws
