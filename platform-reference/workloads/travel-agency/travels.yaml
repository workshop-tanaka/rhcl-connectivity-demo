apiVersion: v1
kind: Service
metadata:
  labels:
    app: travels
  name: travels
  namespace: travel-agency
spec:
  internalTrafficPolicy: Cluster
  ipFamilies:
    - IPv4
  ipFamilyPolicy: SingleStack
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: travels
  sessionAffinity: None
  type: ClusterIP
