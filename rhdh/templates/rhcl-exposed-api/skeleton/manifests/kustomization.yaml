apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# apikey-secret.template.yaml fica de fora de proposito: o valor da chave e
# redigido, entao aplicar por aqui criaria um Secret invalido. Mesma convencao
# dos secret-*.template.yaml da base/ desta demo.

resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - authpolicy.yaml
  - ratelimitpolicy.yaml
